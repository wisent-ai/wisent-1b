{
  "version": "0.1.0",
  "source": "HEAD at 3a0eef5fa9a50ea908beeedaa913dc3516c93efa. Nothing is published for rej-1b: PyPI has no such version, so the only thing anyone can consume is a git install of the version pyproject.toml declares, and this surface is read from the tree that declares it. Regenerate from the sdist once a release exists.",
  "surface": [
    "api:GenerationOutput",
    "api:RejConfig",
    "api:RejConfigV2",
    "api:RejRNM",
    "api:RejRNMv2",
    "api:RejTokenizer",
    "api:generate",
    "api:get_device",
    "api:load_checkpoint",
    "api:rej_1b_config",
    "api:rej_tiny_config",
    "api:rej_tiny_v2_config",
    "api:save_checkpoint",
    "cli:rej-1b-generate",
    "cli:rej-1b-train"
  ]
}
